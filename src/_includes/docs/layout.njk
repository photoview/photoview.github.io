<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} - Photoview Documentation</title>
  <link rel="stylesheet" href="{{ '/assets/main.css' | url }}">
</head>
<body>

  <div class="mx-4 relative">
    <div class="max-w-screen-lg m-auto flex items-start min-h-screen">

    <aside class="w-72 sticky top-0 docs-nav pr-4">
      <nav class="mt-8">
        {% set navPages = collections.all | eleventyNavigation %}
        <ul>
        {%- for entry in navPages %}
          <li{% if entry.url == page.url %} class="my-active-class"{% endif %}>
            <a href="{{ entry.url | url }}">{{ entry.title }}</a>

            {% if entry.children %}
              <ul class="ml-4">
              {%- for subEntry in entry.children %}
                <li>
                  <a href="{{ subEntry.url | url }}">{{ subEntry.title }}</a>
                </li>
              {%- endfor %}
              </ul>
            {% endif %}

          </li>
        {%- endfor %}
        </ul>
      </nav>
    </aside>

    <style>
      .docs-nav::before {
        content: '';
        background-color: rgb(249, 250, 251);
        position: absolute;
        z-index: -1;
        top: 0;
        right: 0;
        width: 100vw;
        height: 100vh;
      }
    </style>

    <main class="mb-40 w-full pl-14 py-14 pr-4">
    {{ content | safe }}
    </main>

    </div>
  </div>
</body>
</html>